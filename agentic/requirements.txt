# Agentowe klastrowanie LLM (Bosch Model Farm) - rdzen pipeline'u
openai>=1.30
PyYAML>=6.0
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
openpyxl>=3.1        # zapis/odczyt .xlsx (badania/buduj_excel_roznic.py, etykiety Rudolfa)

# Tylko dla badania/build_descplus.py (pobieranie opisow z bazy DALI przez SQL Server):
# pyodbc>=5.0

# --- Lokalne klastrowanie hybrydowe (run_local.py) ---
# Tor lokalny NIE potrzebuje zadnych dodatkowych pakietow poza powyzszymi
# (numpy dociagane jest razem z pandas). lightgbm NIE jest juz wymagany -
# warstwa 1 uzywa LinearSVC ze scikit-learn.
#
# Opcjonalne, importowane leniwie - tylko gdy uzyjesz danego trybu:
# torch>=2.0                  # --encoder tfidf+supcon (siec metryczna)
# sentence-transformers>=3.0  # --encoder minilm / bge (pobiera model z HuggingFace)
#
# Sprawdzenie, czego brakuje w TWOIM srodowisku:
#   python sprawdz_srodowisko.py
